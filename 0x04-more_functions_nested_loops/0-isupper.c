#include "main.h"
/**
  *_isupper - checks for uppercase
  *@c: letter to be checked
  *Return: 1 or 0
  */
int _isupper(int c)
{
	if (c <= 65 || C >= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
